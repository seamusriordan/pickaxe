name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build client
      run: |
        cd client
        yarn build
    - name: Build server
      run: |
        cd server
        ./gradlew build
