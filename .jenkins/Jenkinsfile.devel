pipeline {
    agent any

    stages {
        stage('Build and Test') {
            steps {
                sh 'pushd client'
                sh 'gradle build'
                sh 'popd'
            }
        }
    }
}