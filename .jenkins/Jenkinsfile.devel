pipeline {
    agent any

    stages {
        stage('Build and Test') {
            steps {
                dir("client") {
                    sh '../gradlew build'
                }
            }
        }
    }
}