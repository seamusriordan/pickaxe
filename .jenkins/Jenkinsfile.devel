pipeline {
    agent any

    stages {
        stage('Build and Test') {
            steps {
                dir("client") {
                    sh 'ls'
                    sh 'gradle build'
                }
            }
        }
    }
}